{
  "name": "product-passport-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal MVP for generating a product passport report from a fashion product URL.",
  "scripts": {
    "dev": "node server.js",
    "test": "NODE_ENV=test DEEP_READER_WORKER_URL= DEEP_READER_WORKER_TIMEOUT_MS= node --test",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "openai": "^6.42.0",
    "playwright": "^1.60.0"
  }
}
